"""
tools/ — Utility modules for email automation.

Modules:
    email_sender   – Send emails via SMTP (outreach, reply, compose)
    email_fetcher  – Read inbox via IMAP
    resume_parser  – Extract text from PDF + LLM summary
    research_tool  – Search company info via Tavily + LLM summary
"""
